# ────────────────────────────────────────────────────────────────
# Agent SDK — Environment Template
# Copy to .env and fill in your values before running.
# ────────────────────────────────────────────────────────────────

# ── Stellar Wallet ───────────────────────────────────────────────
# ed25519 seed — Strkey format starting with 'S'
# Generate: stellar-sdk keypair generate  or  stellar-cli keys generate
AGENT_SECRET=C22G2DCY443NWBCIMTBA4CSUGTZ4T5HLCCLWJDRCKGMSMNG2KEL6QB7

# ── Stellar Network ──────────────────────────────────────────────
# Testnet
HORIZON_URL=https://horizon-testnet.stellar.org
NETWORK_PASSPHRASE=Test SDF Network ; September 2015
# Mainnet (uncomment when ready)
# HORIZON_URL=https://horizon.stellar.org
# NETWORK_PASSPHRASE=Public Global Stellar Network ; September 2015

# ── Soroban (on-chain agent registry) ───────────────────────────
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
CONTRACT_ID=CDTLE6RKAXDXMKDTBLNXYQFIDQKXFM4ARYBX6DG6XDHJPXRESIJEL3MU

# ── Gas / Fees ───────────────────────────────────────────────────
# Base fee in stroops (100 = 0.00001 XLM per operation)
BASE_FEE_STROOPS=100
# Max slippage tolerance in basis points (50 = 0.5%)
MAX_SLIPPAGE_BPS=50

# ── 0x402 Payment Protocol ───────────────────────────────────────
# URL of the AgentForge platform API (for paying agents via 0x402)
PLATFORM_API_URL=https://agentforge.xyz
# Optional: URL of an external price-feed agent this agent pays for data
PRICE_FEED_AGENT_URL=

# ── QStash (pub-sub backbone) ────────────────────────────────────
QSTASH_URL=https://qstash-eu-central-1.upstash.io
QSTASH_TOKEN=eyJVc2VySUQiOiJmZDZhMGE0ZC1lMmY5LTQ3NWUtYmIzNi05MzVjYzE2MzU3MmUiLCJQYXNzd29yZCI6IjVhMGQzYTIwMDUxZTRhMDNiNDAwNzVhNTA1NzQyZmQzIn0=
QSTASH_CURRENT_SIGNING_KEY=sig_6eSvX3dugsPRfhbSbnB5LG2AnwL3
QSTASH_NEXT_SIGNING_KEY=sig_5EmSqiKD7LDaQGBXQDTKX1k5dapo


# ── Logging ──────────────────────────────────────────────────────
RUST_LOG=info

# ── Arbitrage Tracker ─────────────────────────────────────────────
# Minimum gross rate (1.005 = 0.5% profit threshold)
MIN_PROFIT_RATIO=0.005
MAX_TRADE_XLM=300.0
SCAN_INTERVAL_MS=500
DRY_RUN=true
TX_EXPIRY_SECS=30
# Triangle definitions: semicolon-separated tuples of A:B:C
# XLM = native, others as CODE:ISSUER
ARB_TRIANGLES=XLM:USDC:GBBD:yXLM:GARD
